// Project.xcconfig — shared build settings for all targets.
//
// Apple Silicon (arm64) only: no Intel/x86_64 slices, faster builds, smaller binaries.

ARCHS = arm64
EXCLUDED_ARCHS[sdk=macosx*] = x86_64
ONLY_ACTIVE_ARCH = YES
