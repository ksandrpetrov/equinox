# Equinox: глубокая проверка 25–26 сентября 2026

Продолжение: [повторная проверка 26 сентября](DEEP-QA-ROUND-TWO.md). Она содержит дополнительные исправления и актуальные ограничения выпуска; архив ниже относится к первому проходу.

Статус: исправления и заключительные тесты завершены; код опубликован в `main`. Архив 1.0.0 (5) собран, но загрузка остановлена ошибкой авторизации Xcode. Новая внешняя бета пока не опубликована.

## Среда и границы

- Apple Silicon, macOS 27.0 (26A428), Xcode 27.0 (27A266a).
- Минимальная версия приложения — macOS 26. Живого прогона на macOS 26 нет; по решению пользователя это не блокирует бету.
- Живые операции выполняются в календаре «Из почты», по уточнению пользователя. Удаляются только созданные в этой проверке события с префиксом `Equinox QA`.
- Приложение запускается локально только в Release. Тестовые сервисы используются в XCTest, без чтения/изменения личного календаря тестовым процессом.
- Скриншоты, логи и xcresult находятся в игнорируемом каталоге `build/DeepQA/2026-09-25` и `build/Tests/results`. Скриншоты с личными данными не публикуются в GitHub.

## Подтверждённые дефекты и изменения

| Проблема | Воспроизведение и исправление | Проверка |
| --- | --- | --- |
| Обтекание русских переключателей настроек | В окне минимальной ширины сегменты темы и размера выходили за правую границу. Фиксированная ширина заменена естественной шириной элементов | Графический тест падал до исправления (`run-YYOxHr`), проходит после (`run-vGEDC3`); живые снимки 05/10 |
| Нулевая длительность после all-day | Включить «Весь день», перенести начало, выключить — скрытое время окончания могло оказаться раньше начала. Восстанавливается положительная часовая длительность при сохранении уже валидных интервалов | Тесты EventDraftDefaults, в том числе DST и верхняя граница; живые снимки 14/15 |
| Обратный диапазон в сведениях all-day | Созданное однодневное событие 27 сентября отображалось как «27–26 сентября». EventKit macOS может вернуть конец 23:59:59, а импорт — следующую полночь. Последний день теперь вычисляется корректно для обоих вариантов | EventLayoutTests в UTC, Москве и Лос-Анджелесе; EventKitMutationTests; снимок 17 до исправления |
| Подпись удаления одиночного события | EventKit заполняет occurrenceDate и у одиночных событий. Используются hasRecurrenceRules и isDetached | Проверка несохранённого EventKit-события падала до исправления (`run-2QhEMB`), после проходит (`run-PDJNoC`) |
| Возврат удалённого повторения | При удалении последнего дня конечной серии через `.thisEvent` EventKit macOS восстанавливал ранее исключённый первый день как одиночное событие. Для последнего дня всех правил используется эквивалентное усечение серии; промежуточные и отделённые повторения сохраняют прежний способ удаления | Дважды воспроизведено на серии из двух дней. После исправления: серия 5–7 октября, удаление 5 → 7 → 6, оставшиеся дни соответственно 6/7 → 6 → ни одного. Проверки границ, неизвестных окончаний и DST в Core |
| Лишнее внутреннее скругление и квадратные углы | Убрано внутреннее скругление фона sheet; весь слой содержимого окна обрезается по внешнему скруглению | Живые снимки пользователя и новой панели; графические проверки S/M/L; живые S/light/solid и L/dark/glass |
| Рывок при открытии и закрытии боковой панели | Измерение меняло ширину видимого hosting view до изменения окна. Убрана промежуточная мутация, конечная геометрия ограничивается экраном до единственной анимации | Наблюдение за каждым изменением frame воспроизводило ошибку в S/M/L (`run-s41ZpB`); после исправления проходит (`run-Sqv7yi`) |
| Читаемость подписи лицензии | Подпись MIT License переведена на вторичный уровень контраста | Проверка About |
| Предупреждения собственного тестового кода | Устранены предупреждения о weak-переменной и захвате изменяемых локальных переменных | Новая сборка тестов без этих предупреждений |

По отдельному запросу пользователя создание и просмотр события перенесены в выдвижную панель слева. Высота календаря сохраняется, содержимое формы прокручивается, закрытие — стрелкой или Escape. Кнопка «Готово» убрана, «Добавить» сохраняет событие. Выбор даты использует сетку и компоненты Equinox, с клавиатурной навигацией и вводом года. Подтверждение удаления остаётся отдельным диалогом. Основной календарь временно недоступен при открытой форме; сохранение и удаление защищены от повторных действий.

Ручка сворачивания расположена по центру разделителя: видимая ширина 12 точек, высота 120 в M, область нажатия остаётся 30. Escape сначала закрывает календарь выбора даты, затем форму; введённый текст при первом Escape сохраняется.

## Выполненные прогоны

- Исходная точка: 364 обычных + 8 графических тестов в Debug и Release, 744 выполнения, без ошибок и пропусков (`run-iQrk4V`).
- Исходная точка: Address Sanitizer и Thread Sanitizer — по 364 теста, без ошибок; статический анализ Release завершился успешно.
- Финальное состояние кода сборки 5: 371 обычный + 12 графических тестов в Debug и Release, **766 выполнений, без ошибок и пропусков** (`run-4Yw7FD`). Предыдущие полные прогоны также прошли (`run-iJUh6n`, `run-iY3jxV`).
- Графический запуск с параметром русского языка: 12 без ошибок и пропусков (`graphics-ru-final.xcresult`). Во втором проходе обнаружено, что host оставлял строки framework английскими; этот прогон не подтверждает русский интерфейс. Host исправлен, фактическая локализация и новый русский прогон подтверждены в [повторном отчёте](DEEP-QA-ROUND-TWO.md). Проверяются S/M/L, glass/solid, светлое/тёмное оформление, длинные строки и состояния доступа, загрузки, пустых данных и ошибок.
- Address Sanitizer и Thread Sanitizer окончательной логики: по 371 обычному тесту, без ошибок, пропусков и runtime warnings (`recurrence-asan.xcresult`, `recurrence-tsan.xcresult`). Окончательный интерфейс дополнительно прошёл по 12 графических тестов с каждым sanitizer, также без ошибок, пропусков и runtime warnings (`graphics-asan-final.xcresult`, `graphics-tsan-final.xcresult`). Графический ASan-прогон выполнен на английском.
- Заключительный статический анализ Release успешен (`analyze-recurrence-final.log`). Предупреждений компилятора в собственном коде нет. Xcode сообщает об отсутствии необязательной зависимости AppIntents при извлечении метаданных; macOS 27 также пишет системные диагностические сообщения linkd/Spotlight, присутствующие у изолированного тестового host.
- Геометрия выдвижной панели: проверены ширина, неизменная высота, правый и верхний края в S/M/L (`run-Hxq5uD`). У края экрана положение ограничивается видимой областью.
- Живые проверки: шесть вкладок настроек; создание timed-события с местом и URL; валидация пустого названия и некорректного URL; два daily all-day повторения; распознавание Google Meet и открытие Chrome с ошибкой тестовой ссылки; горячие клавиши формы; сохранение выбора календаря после перезапуска.
- Дополнительно проверены S/light/solid и L/dark/glass, закреплённое окно и popover, 12 переходов по месяцам и возврат «Сегодня», стрелки и Enter в выборе даты. Перенос all-day с 26 на 27 сентября и возврат к timed сохраняет положительный интервал 01:30–02:30. Черновик не сохранялся. Исходные тема, размер, фон, календарный фильтр и отсутствие глобального сочетания восстановлены; закрепление временно сохраняется до проверки установки TestFlight.
- Нагрузочная проверка: 24 быстрых перехода с 4155 синтетическими событиями, Release — 0,418 с суммарно, худший переход 0,048 с; не более двух обращений к подставному хранилищу. Это измерение тестового сценария, а не сетевой синхронизации EventKit.
- Отдельные проверки ошибок, задержек, гонок загрузки, восстановления разрешений, дат, повторов и напоминаний выполняются с CalendarEventStore и несохранёнными объектами EventKit в XCTest.

## Скриншоты

В репозиторий включены только изображения синтетических данных: [новая панель](docs/qa/2026-09-26/drawer.png), [выбор даты](docs/qa/2026-09-26/date-picker.png). Живые снимки 01–25, включая сравнения до/после, хранятся локально. Снимок 21 подтверждает корректную дату однодневного all-day, 22 — тонкую ручку сворачивания, 23 — очистку повторяющихся событий, 24/25 — форму в S/light/solid и L/dark/glass.

## Ограничения живой автоматизации

- Средство управления не подключается к Equinox без видимого окна; панель несколько раз открывал пользователь.
- Приложение macOS «Календарь» не подключается к средству управления. По указанию пользователя используется существующий календарь «Из почты».
- Браузерная политика заблокировала открытие `equinox://…`; обход не выполнялся. Обработка допустимых/ошибочных ссылок проверяется XCTest.
- После локальной пересборки macOS перестала принимать прежнюю временную подпись: журнал TCC подтвердил несовпадение code requirement. Следующая локальная Release-сборка подписана существующим Developer ID через игнорируемый Local.xcconfig. Доступ восстановился и сохраняется при последующих пересборках. Средство управления не имеет права работать с UserNotificationCenter; обход не выполнялся.

## Наблюдение при очистке событий

Повторное появление подтверждено и исправлено, как описано выше. Все созданные серии и timed-событие удалены через Equinox. После исправления последняя серия очищена без повторного удаления и без скрытия записей из отображения (снимок 23).

## Выпуск и очистка

Все изменения опубликованы в GitHub `main`. Исходные незакоммиченные изменения сохранены отдельным коммитом `4837ac7`, основные исправления — `d3bc2de`, исправление удаления последнего повторения — **`6d48a3b25bc675376e97bc3b1ed68c9f50f976f4`**. Обе рабочие ветки (`a.petrov/deep-qa-testflight`, `a.petrov/recurrence-deletion-fix`) безопасно удалены после повторной сверки вершин и проверки включения в опубликованный `main`. На последней сверке локально и на GitHub осталась только `main`; ветки `master` нет.

Из чистого `main` на коммите `6d48a3b` собран окончательный архив `build/Archives/equinox-public-beta-2026-09-26-final.xcarchive`. Проверены архитектура arm64 приложения и встроенного EquinoxKit, минимальная macOS 26, версия 1.0.0 (5), ресурсы, русская локализация, URL scheme, подпись, Sandbox и calendar entitlements. Проверка подписи `--deep --strict` проходит; entitlement `get-task-allow` отсутствует. Заключительный запуск Release из этого же коммита успешен, доступ к календарю сохраняется.

Загрузка с разрешённым внешним тестированием остановилась до отправки пакета: `Failed to Use Accounts`, Xcode не нашёл учётную запись с доступом App Store Connect для команды H23ADBVT65. Журнал: `build/TestFlight/2026-09-26-deep-qa/upload.log`. Запрошено восстановление входа в Xcode. Дополнительно активный сеанс Mac заблокирован, поэтому дальнейшая живая проверка сейчас недоступна.

Временная локальная конфигурация подписи восстановлена из сохранённой копии; она не входит в Git. Подготовлены тексты «Что тестировать» на русском и английском и манифест с коммитом, контрольными суммами архива и результатами тестов в `build/TestFlight/2026-09-26-deep-qa`.

Ожидают завершения: контроль отсутствия тестовых событий после последнего перезапуска, восстановление закрепления, загрузка архива, обработка, установка через внутреннюю группу и публикация в существующей внешней группе Equinox. Это **не ожидание проверки Apple**: сборка 5 ещё не загружена и не отправлена на Beta App Review.

Существующая [публичная ссылка](https://testflight.apple.com/join/N2t7wjvr). App Store Connect повторно проверен 26 сентября: последняя сборка 1.0.0 (4), номер 5 свободен. В проекте назначен 5, версия 1.0.0 сохранена; подпись в project.pbxproj не менялась.
