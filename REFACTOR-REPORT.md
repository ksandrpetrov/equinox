# Дополнительный аудит качества — 12 сентября 2026

## Что изменено

Исходная ревизия — `0f2d88e`. После `git fetch origin --prune` на origin
оказалась только `main`; локальная `refactor/maintainability` уже включена в неё.
Оба рабочих дерева были чистыми. Конфликтов для разрешения не было.
Рабочая ветка этого прохода — `refactor/quality-followup`.

- Исправлена повторная прокрутка agenda после фоновых обновлений. Запрос фокуса
  теперь потребляется один раз после успешной загрузки; простое чтение снимка
  и неуспешная загрузка его не запускают. Ручная прокрутка отменяет ожидание.
  Скрытая панель и выбранный день, отличный от сегодня, не получают этот фокус.
- Владение флагом ожидания полностью перенесено внутрь координатора навигации;
  убрана пара внешних операций «сбросить флаг / снова запросить прокрутку».
- Упрощены построение цветовых точек календарей и добавление событий в словари:
  удалены недостижимая ветка и повторное конструирование готового массива,
  использован стандартный subscript словаря с default.
- Подставной CalendarEventStore поддерживает отказ доступа, отдельные результаты
  fetch/refetch, учёт запросов и ошибку в снимке. Добавлены семь сценарных тестов
  восстановления и навигации.
- Графические проверки обнаруживают пустой/однотонный рендер и выход fittingSize
  за выделенные границы; цвета поверхностей проверяются реальным рендером
  в светлой и тёмной темах.

Публичные API, UserDefaults-ключи, deep links, TCC, ресурсы, UI-компоненты,
зависимости и настройки подписи не изменены. Новых исходных файлов и targets нет.

## Почему так

Регрессионный тест на исходном коде воспроизвёл четыре неверных изменения токена
прокрутки: при чтении снимка, при отказе загрузки и при последующих обновлениях.
Причина — старый обработчик сбрасывал ожидание и сразу повторно устанавливал его
через requestAgendaScroll. Результат fetch при этом игнорировался.

Сохранение события и последующая загрузка проверяются отдельно: успешная запись
не должна превращаться в ошибку сохранения при отказе обновления, иначе повторное
нажатие пользователем может создать дубликат. Новые тесты подтверждают, что Retry
повторяет только загрузку; успешное удаление закрывает сведения даже при отказе
последующего обновления. Эти контракты сохранены, а не изменены.

### Пересмотр тестов

- Удалён testEventKitCalendarMappingUsesColorHex: тот же адаптер проверяется
  testColorHexFormatsSRGB в EventKitCalendarMappingTests; преобразования разных
  цветовых пространств остаются в ColorHexTests.
- Два теста количества SwiftUI-точек заменены одним тестом конкретных цветов,
  порядка первых календарей, удаления повторов и ограничения до трёх точек.
  Чистые тесты выбора календарей и пустого ввода сохранены.
- testSurfaceTokensUseAdaptiveSystemColors заменён графическим тестом: отсутствие
  нескольких строк в исходнике не доказывало адаптацию цвета к теме.
- Удалён testNoReferencesToRemovedMenuBarAssets — запрет исторических имён.
  Все действующие варианты menu bar продолжают проходить реальный рендер.
- Два повторяющихся сканера исходников используют существующий assertNoOccurrences;
  неиспользуемое поле designPath удалено. Остальные проверки ограничений дизайна,
  доступности и локализации сохранены.

### Матрица сценариев и оставшихся пробелов

Инвентаризация охватила 96 исходников: App — 14, Core — 23, Services — 19,
UI — 40; просмотрен состав всех тестовых наборов. Выполнены общая проверка
границ слоёв, ресурсов и targets и углублённый разбор загрузки, навигации,
настроек, календарного адаптера, управления панелью и затронутых тестов.
Это не доказательство отсутствия любых дефектов во всём проекте.

| Сценарий | Автоматическая проверка | Пробел / предел проверки |
|---|---|---|
| Даты, сетка, границы лет, DST | Арифметика дат, матрицы сетки, лейаут многодневных событий, черновики | Реальные системные настройки на минимальной macOS не проверялись |
| Fetch, кэш, смена календарей и доступа | Очередь, поколения, запоздалые снимки, отказ и повторная загрузка | Нет живого теста EventKit fetch с изменением TCC |
| Создание и удаление | Валидация, несохранённые EKEvent, ошибки мутаций; добавлены отказы последующего reload | Пользовательская база не изменялась |
| Agenda и навигация | Фокус, диапазоны, границы, ручная прокрутка; добавлен одноразовый фокус после fetch | Жесты в живом приложении не проверены |
| Meeting URLs, RSVP, индикатор | Реестр провайдеров, обнаружение, native fallback, участие, время | Внешние приложения встреч не запускались |
| Настройки | Persistence, нормализация, reset, уведомления, ошибки автозапуска | Системный автозапуск не переключался |
| Панель, hotkeys, системное время | Состояния панели, Escape, capture lifecycle, уведомления, таймер | Живое переключение pin/popover и глобальная горячая клавиша не проверены |
| UI и локализация | S/M/L, light/dark, шесть вкладок, ресурсы и переводы; усилена геометрия и рендер | Рендер не проверяет все взаимодействия, VoiceOver и системный toolbar |

## Проверки

Среда: Apple Silicon, macOS 27.0 (26A428), Xcode 26.6 (17F113).

| Проверка | Результат |
|---|---|
| Исходный `./scripts/test.sh` | Debug и Release: 341 unit + 5 graphics, 0 ошибок, 0 пропусков |
| Регрессионный тест до исправления | 1 тест упал с 4 assertions, 2 соседних прошли |
| Промежуточный `./scripts/test.sh --configuration Debug` | 344 unit + 6 graphics, 0 ошибок, 0 пропусков |
| Итоговый `./scripts/test.sh` | Debug и Release: 344 unit + 6 graphics, 0 ошибок, 0 пропусков |
| `./run.sh` | Release успешно собран и запущен |
| `codesign --verify --deep --strict build/DerivedData/Build/Products/Release/equinox.app` | Успешно |
| `git diff --check`, `bash -n` для run.sh и всех shell-скриптов | Успешно |
| `plutil -lint` для plist/strings/stringsdict/entitlements; JSON ресурсов | Успешно |
| Границы импортов Core/UI, состав targets | Нарушений не найдено; все 95 исходников EquinoxKit включены, дублей в targets нет |
| `git diff --quiet -- equinox.xcodeproj/project.pbxproj` | Проект и signing не изменены |

Первая попытка исходного прогона в sandbox остановилась до компиляции из-за
недоступных кэшей SwiftPM и служб Xcode. Все зачтённые прогоны выполнены после
разрешённого запуска вне sandbox; ошибка среды не считается падением тестов.

Результаты под `build/Tests/results/`:
- исходный: `run-OiLeUD`;
- воспроизведение: `run-TOeDVX`;
- промежуточный Debug: `run-ND2Y77`;
- итоговый: `run-GFf0jU`.

Release-покрытие строк EquinoxKit, исходное → итоговое:

| Слой | Unit | Graphics |
|---|---:|---:|
| Core | 91,47% → 91,43% | 33,76% → 34,01% |
| App | 71,07% → 73,57% | 39,89% → 39,55% |
| Services | 59,20% → 59,06% | 19,59% → 19,66% |
| UI | 4,24% → 4,24% | 65,48% → 65,48% |

Покрытие App выросло за счёт отказов и восстановления. Небольшие изменения других
долей связаны с удалением исполняемых строк и изменением путей исполнения.
Проценты двух наборов не складываются и не измеряют полноту бизнес-сценариев.

Время исполнения XCTest из логов (без сборки), исходное → итоговое:
Debug unit 0,760 → 0,926 с; graphics 1,741 → 2,235 с;
Release unit 0,887 → 0,862 с; graphics 1,757 → 1,892 с.
Одиночные прогоны не доказывают ускорение или замедление; графические проверки
теперь выполняют дополнительный анализ изображения. Полное время зависит от
кэшей сборки и запуска test host.

## Риски

Computer Use дважды вернул timeout, в том числе при обращении к точному пути
собранного Release. Промежуточное обращение по bundle ID обнаружило несколько
копий приложения. Поэтому живая UI-приёмка не считается выполненной.
Просмотрены четыре итоговых изображения: панель S/light и M/dark, создание
события L/light и Appearance/dark. В снимке невидимого settings-host выделение
sidebar и системный toolbar воспроизводятся неполно; графические assertions
не заменяют проверку активного окна.

## Что не сделано

Не добавлены CI, зависимости и новые функции. Не выполнялись запись/удаление
в пользовательском календаре, изменение системного доступа, полная ручная
UI-приёмка, проверка на macOS 26.0 и нотаризация.

---

Ниже сохранён отчёт предыдущего прохода; его результаты относятся к прежней ревизии.

# Рефакторинг и проверка equinox — 12 сентября 2026

## Что изменено

Работа основана на `main` (`68e8e9f`) и существовавших локальных изменениях.
После `fetch` на origin была только `main`; других веток для объединения не было.
Исходные правки и новые файлы сохранены в резервной копии вне репозитория.

- Код приложения выделен в `EquinoxKit.framework`; GUI содержит точку входа.
  Обычные XCTest не запускают приложение. Графические проверки имеют отдельный
  минимальный host без календарного AppDelegate.
- Библиотека явно владеет локализацией и ресурсами через `Bundle.equinox`.
  Единственный новый публичный тип — `AppDelegate`; модели остаются внутренними.
  Идентичность приложения, ключи настроек и deep links сохранены.
- Контракт `CalendarEventStore` отделён от реализации EventKit, черновик события
  и ошибки перенесены в `Core`. Выбор календарей проверяется на снимках значений.
- Инициализация состояния завершается до запроса доступа; тесты ожидают её явно.
  Время и отложенные действия индикатора загрузки подставляются в тестах.
  Убраны реальные задержки и циклы ожидания из фиксированного числа `Task.yield()`.
- Сброс настроек использует внедрённые хранилище и платформенные операции.
  Видимость закреплённой панели сохраняется через `PreferencesStore`.
- Удалены неиспользуемый `shortWeekday`, промежуточный календарный обработчик
  фасада и тривиальная обёртка `shouldPersistSelection`.
- Смешанные тестовые файлы разделены по компонентам, общие фикстуры вынесены.
  `scripts/test.sh` использует только Xcode, сохраняет результаты и покрытие,
  завершает работу с ошибкой при пустом прогоне, пропусках или падениях.
  Ручной Python-компилятор удалён. Версия KeyboardShortcuts 3.0.1 закреплена в Git.

Проверены календарная логика, очередь загрузки и снимки, настройки, адаптеры,
жизненный цикл панели, ресурсы, локализация, сборочные targets и тесты. Изменения
UI в основном задают bundle ресурсов; редизайн не выполнялся.

## Почему так

Рефакторинг сохраняет существующие слои и `CalendarStore` actor, но убирает
дублирующий способ сборки и зависимости тестов от запуска продукта. Новых
пакетов и CI нет. Xcode остаётся источником состава targets.

Дополнительно исправлены подтверждённые сценарии:

| Сценарий | Изменение | Проверка |
|---|---|---|
| Пересекающиеся загрузки | Новый запрос не сдвигает срок появления индикатора; завершение немедленно обновляет fetching-state | Управляемое время, отменённые callbacks, вложенные запросы |
| All-day в EventKit | Даты задаются перед `isAllDay`, чтобы исключительная конечная дата не стала дополнительным днём | Дефект воспроизведён на несохранённом EKEvent; двухдневное событие разбивается ровно на два дня |
| Временное исчезновение календарей | Выбор сохраняется; пустое имя источника не теряет заголовок группы | Снимки discovery, deselect, повторная загрузка настроек |
| Сброс настроек | Платформенные операции подставляются; видимость панели сбрасывается в том же хранилище | Успех и ошибка автозапуска, сохранение после повторного создания PreferencesStore |
| Запуск упакованного приложения | Framework использует `@rpath`; `run.sh` проверяет наличие процесса после запуска | Живой Release-запуск, `otool`, проверка подписи |

Существовавшие локальные исправления дат, URL, agenda, геометрии панели,
клавиатурного фокуса и синхронизации сохранены и включены в общий прогон.
Их исходные отчёты: `AUDIT.md`, `DESIGN-AUDIT.md`, `OFFLINE-TEST-REPORT.md`.

## Удалённые и заменённые тесты

- Удалён `testJulianRoundTrip`: одну дату уже покрывает исчерпывающий round-trip
  всех 639 540 поддерживаемых дней.
- Удалены `testDesignTokensExposeMenuBarTokens` и
  `testDesignTokensExposeNewComplianceTokens`: они повторяли значения констант.
  Проверки контраста, читаемости, ресурсов и ограничений дизайн-системы сохранены.
- Три точечных теста сетки заменены двумя матрицами месяцев, начала недели,
  6–10 строк, непрерывности дат и границ выделения.
- Тест булевой обёртки сохранения календарей заменён проверками реального
  изменения выбора и временного исчезновения календарей.
- Тесты индикатора переписаны на управляемое время; тест высоты toolbar проверяет
  минимальный размер, а не точную копию констант для S/M/L.
- Графический тест перенесён в отдельный target, его прежний skip удалён.

## Проверки

Среда: macOS 27.0 (26A428), Apple Silicon, Xcode 26.6 (17F113).

| Прогон | Результат |
|---|---|
| Исходное рабочее дерево, офлайн-XCTest Debug | 340 тестов: 339 прошли, 1 графический пропущен |
| Итоговый XCTest Debug | 341 обычный + 5 графических, 0 ошибок, 0 пропусков |
| Итоговый XCTest Release | 341 обычный + 5 графических, 0 ошибок, 0 пропусков |
| `./run.sh` | Release собран и запущен; процесс оставался работающим после проверки |
| `codesign --verify --deep --strict` | Успешно для собранного приложения |
| Состав targets | Все 95 исходников библиотеки, 41 обычный тестовый файл и 4 файла графического target включены без дубликатов внутри target |
| Дополнительные проверки | `git diff --check`, `bash -n`, plist/strings/stringsdict/entitlements и JSON ресурсов — успешно |
| Подпись в проекте | Исходные `DEVELOPMENT_TEAM` / `ProvisioningStyle` не изменены |

Команда финального прогона:

```bash
EQUINOX_TEST_DERIVED_DATA=build/NativeTests \
EQUINOX_PACKAGE_CACHE=/Users/aleksandr/Developer/equinox/build/DerivedData/SourcePackages \
./scripts/test.sh
```

Без переопределений достаточно `./scripts/test.sh`.
Результаты этого запуска: `build/NativeTests/results/run-E1dIy6/` в рабочем дереве
рефакторинга. Файлы сборки и `.xcresult` не коммитятся.

Покрытие строк в Release по отчётам `xccov`, отдельно для двух наборов:

| Слой | Обычные тесты | Графические тесты |
|---|---:|---:|
| Core | 91,5% | 33,8% |
| App | 71,1% | 39,9% |
| Services | 59,2% | 19,6% |
| UI | 4,2% | 65,5% |

Проценты нельзя складывать: наборы частично исполняют одни и те же строки.
SwiftUI и макросы влияют на число исполняемых строк. Эти значения не означают
полное покрытие всех пользовательских сценариев.

В процессе проверки исправлена ошибка конфигурации framework: успешный XCTest
не обнаруживал абсолютный install-name, мешавший запуску GUI. После перехода на
`@rpath` приложение запущено, оба набора повторно прошли Debug и Release.

Визуально просмотрены выбранные изображения компонентов, панели, формы создания
и настроек. Просмотр выявил ошибку подготовки выбранной вкладки в новой фикстуре;
она исправлена, все вкладки проверены повторно. Снимки невидимых NSHostingView
могут не воспроизводить системный toolbar и материал выделения активного окна.

## Риски

Живая визуальная приёмка ограничена: Computer Use неоднократно возвращал timeout.
Запуск и подпись проверены, но реальное переключение pin/popover, ввод в sheets,
системные диалоги TCC, VoiceOver и работа горячих клавиш в активном приложении
не считаются проверенными только на основании рендеров.

EventKit-тесты создают несохранённые объекты; при этом сам framework может
обращаться к системным настройкам напоминаний и писать сообщения об отсутствии
доступа. Тесты не запрашивают разрешение и не выполняют fetch/save/remove.
Реальное создание и удаление событий в пользовательской базе не выполнялось.
Проверка на минимальной macOS 26.0 остаётся необходимой перед распространением.

## Что не сделано

Не добавлены CI, сторонние зависимости и новые продуктовые функции. Нотаризация,
публикация установщика и полная ручная приёмка не выполнялись. Исторические
отчёты не используются как доказательство текущего состояния.
