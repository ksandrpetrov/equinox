import KeyboardShortcuts

extension KeyboardShortcuts.Name {
    /// Persists under `KeyboardShortcuts_GlobalShortcut` (legacy MASShortcut key: `GlobalShortcut`).
    static let togglePanel = Self(kKeyboardShortcut)
}
